"""Perf-harness package (Phase 3 deliverable 1).

Shared measurement code used by BOTH ``tests/perf/test_budgets.py`` (the CI
budget gate) and ``scripts/perf_check.py`` (the regenerable baseline tool) —
one implementation, two entry points, no drift.
"""
