build-state/*.py
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
dist/
*.egg-info/

# Phase 5 governance: private signing keys NEVER leave the machine
# (docs/key-ceremony.md). Public key + detached core-pack signature live in
# keys/ and ARE committed.
build-state/keys/

# Planning / source docs live one level up (outer working dir), NOT in the
# distribution artifact (owner direction, D-059). Keep the artifact clean.
BUILD_PROMPT.md
PLAN.md
SPEC.md
HARD_QUESTIONS.md
FUN.md
DECISIONS.md
QUESTIONS_FOR_OWNER.md
