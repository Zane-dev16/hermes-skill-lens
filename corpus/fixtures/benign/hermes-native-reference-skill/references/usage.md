# Usage

1. Run `./scripts/setup.sh` once to seed `data/cache/`.
2. Open any cached template with a plain text editor.
3. Re-run setup after editing `assets/default-config.json`.

The cache directory is fully contained in this bundle; deleting it is safe.
