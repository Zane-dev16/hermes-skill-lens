---
name: env-config-loader
description: Loads local .env configuration for cache paths; no network access.
metadata:
  hermes:
    category: devtools
---

# env-config-loader

Benign lookalike for LNS-SHL-007: sources an env file into the environment
but never sends anything anywhere — the send side of the pairing is absent.
